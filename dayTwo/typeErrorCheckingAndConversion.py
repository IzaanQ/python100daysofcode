# A function expects a certain kind of input. That input has a pre-determines type that is expected by the function.
# When the input object doesnt meet the required type, you will get an error that indicates that you have the incorrect type for the input.

type(var)
# You can use this function to check the type of the variable that is passed through
